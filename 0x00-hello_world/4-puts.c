#include <stdio.h>

/**
 * main - the main function
 * 
 * Return: 0 (Success)
 */

int main(void)
{
        puts("\"Programming is like building a multilingua        l puzzle"):
        return (0);
}
